spring:
  cloud:
    nacos:
      config:
        server-addr: 121.196.245.235:8848
        group: sz
        file-extension: yml
        # shared-configs: common.yml
  application:
      name: order-service
  profiles:
    active: dev

